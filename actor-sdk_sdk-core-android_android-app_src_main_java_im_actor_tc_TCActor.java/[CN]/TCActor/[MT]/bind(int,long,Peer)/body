{
  ids.add(id);
  peerMap.put(id,peer);
  if (reidMap.get(id) != null) {
    reidMap.get(id).add(rid);
  }
 else {
    ArrayList<Long> rids=new ArrayList<>();
    rids.add(rid);
    reidMap.put(id,rids);
  }
}
