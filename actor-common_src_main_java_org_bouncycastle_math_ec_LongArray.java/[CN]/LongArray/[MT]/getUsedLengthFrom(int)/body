{
  long[] a=m_ints;
  from=Math.min(from,a.length);
  if (from < 1) {
    return 0;
  }
  if (a[0] != 0) {
    while (a[--from] == 0) {
    }
    return from + 1;
  }
  do {
    if (a[--from] != 0) {
      return from + 1;
    }
  }
 while (from > 0);
  return 0;
}
