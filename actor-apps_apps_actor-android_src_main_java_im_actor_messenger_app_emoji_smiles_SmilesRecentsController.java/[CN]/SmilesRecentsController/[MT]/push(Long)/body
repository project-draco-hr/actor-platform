{
  if (buffer == null) {
    buffer=new ArrayList<Long>(this);
  }
  if (buffer.contains(object)) {
    buffer.remove(object);
  }
  buffer.add(0,object);
  while (buffer.size() > 32) {
    buffer.remove(32);
  }
}
