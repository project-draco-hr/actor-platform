{
  messages.clear();
  docs.clear();
  inPendingIndex.clear();
  outPendingIndex.clear();
  dialogsActor.send(new DialogsActor.ChatClear(peer));
  state=state.changeInReadDate(0).changeInMaxDate(0);
  conversationState.addOrUpdateItem(state);
}
