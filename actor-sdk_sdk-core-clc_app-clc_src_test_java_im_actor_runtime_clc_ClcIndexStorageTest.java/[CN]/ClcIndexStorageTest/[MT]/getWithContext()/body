{
  is.put(1,20);
  srp.setContext("935");
  is=srp.createIndex("test");
  is.put(1,30);
  assertEquals(is.get(1),new Long(30));
  srp.setContext(null);
  is=srp.createIndex("test");
  assertEquals(is.get(1),new Long(20));
}
