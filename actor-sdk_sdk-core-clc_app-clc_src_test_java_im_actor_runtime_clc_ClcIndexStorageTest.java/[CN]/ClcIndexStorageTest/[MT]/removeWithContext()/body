{
  is.put(1,10);
  is.put(2,20);
  srp.setContext("935");
  is=srp.createIndex("test");
  is.put(1,30);
  is.put(2,40);
  is.remove(1);
  assertNull(is.get(1));
  srp.setContext(null);
  is=srp.createIndex("test");
  assertEquals(is.get(1),new Long(10));
}
