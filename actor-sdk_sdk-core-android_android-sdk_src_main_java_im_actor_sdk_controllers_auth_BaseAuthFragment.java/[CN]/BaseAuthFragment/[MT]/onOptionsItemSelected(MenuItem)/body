{
  int i=item.getItemId();
  if (i == R.id.sign_in) {
    startSignIn();
    return true;
  }
 else   if (i == R.id.sign_up) {
    startSignUp();
    return true;
  }
 else {
    return super.onOptionsItemSelected(item);
  }
}
