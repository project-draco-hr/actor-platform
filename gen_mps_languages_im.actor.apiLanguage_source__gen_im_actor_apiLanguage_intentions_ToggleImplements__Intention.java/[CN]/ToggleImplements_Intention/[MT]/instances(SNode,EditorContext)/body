{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new ToggleImplements_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
