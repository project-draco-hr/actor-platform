{
  if (engine == null) {
synchronized (lock) {
      if (engine == null) {
        SQLiteStorageAdapter storageAdapter=new SQLiteStorageAdapter(SQLiteProvider.db(),tableName,dataAdapter instanceof FilterableDataAdapter);
        engine=new ListEngine<T>(storageAdapter,dataAdapter);
        uiListEngine=new EngineUiList<T>(engine);
      }
    }
  }
  return engine;
}
