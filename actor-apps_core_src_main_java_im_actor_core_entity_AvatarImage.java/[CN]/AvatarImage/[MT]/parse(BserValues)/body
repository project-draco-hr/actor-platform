{
  if (values.getBool(5,false)) {
    super.parse(values);
  }
 else {
    ObsoleteAvatarImage obsoleteAvatarImage=new ObsoleteAvatarImage(values);
    setWrapped(new ApiAvatarImage(new FileLocation(obsoleteAvatarImage.getFileReference().getFileId(),obsoleteAvatarImage.getFileReference().getAccessHash()),obsoleteAvatarImage.getWidth(),obsoleteAvatarImage.getHeight(),obsoleteAvatarImage.getFileReference().getFileSize()));
  }
}
