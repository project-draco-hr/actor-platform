{
  Group group=groups().getValue(groupId);
  if (group != null) {
    if (!group.getTitle().equals(title)) {
      Group upd=group.editTitle(title);
      groups().addOrUpdateItem(upd);
      onGroupDescChanged(upd);
    }
    if (!isSilent) {
      Message message=new Message(rid,date,date,uid,uid == myUid() ? MessageState.SENT : MessageState.UNKNOWN,ServiceGroupTitleChanged.create(title));
    }
  }
}
