{
  ApiGroup w=getWrapped();
  ApiGroup res=new ApiGroup(w.getId(),w.getAccessHash(),w.getTitle(),avatar,w.getCreatorUid(),w.getMembers(),w.getCreateDate(),w.getTheme(),w.getAbout(),w.isHidden(),w.getGroupType(),w.getExt());
  res.setUnmappedObjects(w.getUnmappedObjects());
  return new Group(res);
}
