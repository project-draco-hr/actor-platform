{
  ApiGroup w=getWrapped();
  ApiGroup res=new ApiGroup(w.getId(),w.getAccessHash(),title,w.getAvatar(),w.getCreatorUid(),w.getMembers(),w.getCreateDate(),w.getTheme(),w.getAbout(),w.isHidden(),w.getGroupType(),w.getExt());
  res.setUnmappedObjects(w.getUnmappedObjects());
  return new Group(res);
}
