{
  ApiGroup w=getWrapped();
  ApiGroup res=new ApiGroup(w.getId(),w.getAccessHash(),w.getTitle(),w.getAvatar(),w.getCreatorUid(),w.getMembers(),w.getCreateDate(),w.getTheme(),about,w.isHidden(),w.getGroupType(),w.getExt());
  res.setUnmappedObjects(w.getUnmappedObjects());
  return new Group(res);
}
