{
  ApiGroup w=getWrapped();
  ApiGroup res=new ApiGroup(w.getId(),w.getAccessHash(),w.getTitle(),w.getAvatar(),w.getMembersCount(),w.isHidden(),w.getExt(),w.isMember(),w.isAdmin(),w.getCreatorUid(),w.getMembers(),w.getCreateDate(),w.getTheme(),about);
  res.setUnmappedObjects(w.getUnmappedObjects());
  return new Group(res);
}
