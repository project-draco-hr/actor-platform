{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new DangerParameter_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
