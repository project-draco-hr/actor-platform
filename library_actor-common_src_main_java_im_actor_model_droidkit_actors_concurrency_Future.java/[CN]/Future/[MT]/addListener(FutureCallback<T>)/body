{
  callbacks.add(callback);
  if (isCompleted) {
    if (isCanceled) {
      return true;
    }
    if (isError) {
      callback.onError(error);
    }
 else {
      callback.onResult(result);
    }
  }
  return isCompleted;
}
