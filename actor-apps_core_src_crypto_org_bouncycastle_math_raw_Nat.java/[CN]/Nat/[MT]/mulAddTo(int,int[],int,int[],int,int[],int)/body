{
  long zc=0;
  for (int i=0; i < len; ++i) {
    long c=mulWordAddTo(len,x[xOff + i],y,yOff,zz,zzOff) & M;
    c+=zc + (zz[zzOff + len] & M);
    zz[zzOff + len]=(int)c;
    zc=c >>> 32;
    ++zzOff;
  }
  return (int)zc;
}
