{
  ApiUser w=getWrapped();
  ApiUser res=new ApiUser(w.getId(),w.getAccessHash(),w.getName(),w.getLocalName(),w.getSex(),avatar,w.getContactInfo(),w.isBot(),w.getNick(),w.getAbout(),w.getExternal());
  res.setUnmappedObjects(w.getUnmappedObjects());
  return new User(res);
}
