{
  ApiUser w=getWrapped();
  ApiUser res=new ApiUser(w.getId(),w.getAccessHash(),w.getName(),localName,w.getSex(),w.getAvatar(),w.getContactInfo(),w.isBot(),w.getNick(),w.getAbout());
  res.setUnmappedObjects(w.getUnmappedObjects());
  return new User(res);
}
