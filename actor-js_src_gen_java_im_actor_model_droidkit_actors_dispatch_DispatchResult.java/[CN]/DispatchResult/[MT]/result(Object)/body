{
  DispatchResult result=FREE_RESULTS.get();
  if (result != null) {
    FREE_RESULTS.remove();
    result.update(true,res,0);
  }
 else {
    result=new DispatchResult(true,res,0);
  }
  return result;
}
