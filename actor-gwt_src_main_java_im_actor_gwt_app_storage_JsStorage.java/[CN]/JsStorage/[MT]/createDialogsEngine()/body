{
  return new JsListEngine<Dialog>("dialogs",storage){
    @Override protected byte[] serialize(    Dialog item){
      return item.toByteArray();
    }
    @Override protected Dialog deserialize(    byte[] data){
      try {
        return Dialog.fromBytes(data);
      }
 catch (      IOException e) {
        e.printStackTrace();
        return null;
      }
    }
  }
;
}
