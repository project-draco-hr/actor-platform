{
  return new JsListEngine<Contact>("??ontacts",storage){
    @Override protected byte[] serialize(    Contact item){
      return item.toByteArray();
    }
    @Override protected Contact deserialize(    byte[] data){
      try {
        return Contact.fromBytes(data);
      }
 catch (      IOException e) {
        e.printStackTrace();
        return null;
      }
    }
  }
;
}
