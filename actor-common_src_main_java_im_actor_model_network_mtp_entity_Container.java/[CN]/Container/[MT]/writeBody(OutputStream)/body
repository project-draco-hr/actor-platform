{
  if (messages != null && messages.length > 0) {
    writeVarInt(messages.length,bs);
    for (    ProtoMessage m : messages) {
      m.writeObject(bs);
    }
  }
 else {
    writeVarInt(0,bs);
  }
}
