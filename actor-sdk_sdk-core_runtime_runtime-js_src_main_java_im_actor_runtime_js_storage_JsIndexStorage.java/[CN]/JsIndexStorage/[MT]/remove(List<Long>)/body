{
  ArrayList<Item> toRemove=new ArrayList<Item>();
  for (  Item itm : items) {
    for (    Long k : keys) {
      if (k.equals(itm.getKey())) {
        toRemove.add(itm);
        break;
      }
    }
  }
  if (toRemove.size() > 0) {
    items.removeAll(toRemove);
    save();
  }
}
