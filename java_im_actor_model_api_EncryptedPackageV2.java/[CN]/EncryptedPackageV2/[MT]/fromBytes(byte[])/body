{
  DataInput input=new DataInput(src,0,src.length);
  int key=input.readInt();
  byte[] content=input.readProtoBytes();
switch (key) {
case 1:
    return Bser.parse(new EncryptedMessageV2(),content);
default :
  return new EncryptedPackageV2Unsupported(key,content);
}
}
