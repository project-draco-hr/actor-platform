{
synchronized (LOCK) {
    for (    long key : keys) {
      cache.removeObject(key);
    }
    storage.delete(keys);
    for (    ListEngineDisplayListener<T> l : listeners) {
      l.onItemsRemoved(keys);
    }
  }
}
