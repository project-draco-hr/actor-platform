{
  String attrs="";
  for (  String key : attributes.keySet()) {
    if (attrs.length() > 0) {
      attrs+=", ";
    }
    attrs+=key + " = " + attributes.get(key);
  }
  if (attrs.length() > 0) {
    return "[" + type + "]("+ attrs+ ")";
  }
  return "[" + type + "]";
}
