{
  builder=new GenericDraweeHierarchyBuilder(getResources());
  GenericDraweeHierarchy hierarchy=builder.setFadeDuration(200).setActualImageScaleType(ScalingUtils.ScaleType.CENTER_INSIDE).build();
  setHierarchy(hierarchy);
  fastThumbLoader=new FastThumbLoader(this);
}
