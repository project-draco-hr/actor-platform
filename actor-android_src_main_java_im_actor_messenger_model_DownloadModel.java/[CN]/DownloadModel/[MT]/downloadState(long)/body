{
synchronized (uploadModels) {
    if (!uploadModels.containsKey(rid)) {
      uploadModels.put(rid,new ValueModel<DownloadState>("upload." + rid,new DownloadState(DownloadState.State.NONE,0)));
    }
    return uploadModels.get(rid);
  }
}
