{
  if (message instanceof SeqUpdate) {
    SeqUpdate seqUpdate=(SeqUpdate)message;
    try {
      onSeqUpdate(seqUpdate.type,seqUpdate.body,seqUpdate.users,seqUpdate.groups).pipeTo(resolver);
    }
 catch (    Exception e) {
      resolver.tryError(e);
    }
  }
 else   if (message instanceof DifferenceUpdate) {
    DifferenceUpdate differenceUpdate=(DifferenceUpdate)message;
    onDifferenceUpdate(differenceUpdate.getDifference()).pipeTo(resolver);
  }
 else {
    super.onAsk(message,resolver);
  }
}
