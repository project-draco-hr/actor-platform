{
  super.preStart();
  endpoints=new Endpoints(new ConnectionEndpoint[]{new ConnectionEndpoint("front1-mtproto-api-rev3.actor.im",443,ConnectionEndpoint.Type.TCP),new ConnectionEndpoint("front2-mtproto-api-rev3.actor.im",443,ConnectionEndpoint.Type.TCP)},new TrustedKey[0]);
  keyCreation=system().actorOf(Props.create(new ActorCreator(){
    @Override public AuthKeyActor create(){
      return new AuthKeyActor();
    }
  }
),getPath() + "/test");
}
