{
  if (engine == null) {
synchronized (lock) {
      StorageAdapter<T> storageAdapter=new SQLiteStorageAdapter<T>(DbProvider.getDatabase(AppContext.getContext()),databaseName,adapter);
      engine=new KeyValueEngine<T>(storageAdapter,adapter);
    }
  }
  return engine;
}
