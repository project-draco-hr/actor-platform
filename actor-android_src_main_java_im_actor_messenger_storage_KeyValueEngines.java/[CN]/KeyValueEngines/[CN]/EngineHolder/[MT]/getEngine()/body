{
  if (engine == null) {
synchronized (lock) {
      StorageAdapter<T> storageAdapter=new SQLiteStorageAdapter<T>(SQLiteProvider.db(),databaseName,adapter);
      engine=new KeyValueEngine<T>(storageAdapter,adapter);
    }
  }
  return engine;
}
