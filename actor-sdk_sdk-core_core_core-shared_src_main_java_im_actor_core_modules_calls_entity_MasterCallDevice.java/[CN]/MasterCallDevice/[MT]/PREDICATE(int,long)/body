{
  return new Predicate<MasterCallDevice>(){
    @Override public boolean apply(    MasterCallDevice masterCallDevice){
      return masterCallDevice.getUid() == uid && masterCallDevice.getDeviceId() == deviceId;
    }
  }
;
}
