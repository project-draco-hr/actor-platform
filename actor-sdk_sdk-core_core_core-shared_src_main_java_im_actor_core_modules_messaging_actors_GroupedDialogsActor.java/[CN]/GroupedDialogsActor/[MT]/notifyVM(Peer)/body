{
  boolean found=false;
  for (  GroupedItem i : storage.getGroups()) {
    for (    Peer p : i.getPeers()) {
      if (p.equals(peer)) {
        found=true;
        break;
      }
    }
  }
  if (found) {
    notifyVM();
  }
}
