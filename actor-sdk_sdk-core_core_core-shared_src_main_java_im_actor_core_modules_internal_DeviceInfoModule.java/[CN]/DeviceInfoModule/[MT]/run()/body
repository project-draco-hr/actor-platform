{
  actorRef=system().actorOf(Props.create(DeviceInfoActor.class,new ActorCreator<DeviceInfoActor>(){
    @Override public DeviceInfoActor create(){
      return new DeviceInfoActor(context());
    }
  }
),"device_info/notifier");
}
