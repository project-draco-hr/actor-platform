{
  Message message=new Message(RandomUtils.nextRid(),date,date,uid,MessageState.UNKNOWN,new ServiceUserRegistered());
  messenger.getMessagesModule().getConversationActor(Peer.user(uid)).send(message);
}
