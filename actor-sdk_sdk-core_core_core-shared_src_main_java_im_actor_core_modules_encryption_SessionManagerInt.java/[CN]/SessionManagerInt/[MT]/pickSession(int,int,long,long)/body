{
  return ask(new SessionManagerActor.PickSessionForDecrypt(uid,keyGroup,theirKeyId,ownKeyId)).map(new Function<SessionManagerActor.PickSessionResp,PeerSession>(){
    @Override public PeerSession apply(    SessionManagerActor.PickSessionResp src){
      return src.getSession();
    }
  }
);
}
