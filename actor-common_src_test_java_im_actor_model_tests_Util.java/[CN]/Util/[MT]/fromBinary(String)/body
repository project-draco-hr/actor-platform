{
  byte[] res=new byte[src.length() / 8];
  for (int i=0; i < res.length; i++) {
    int val=0;
    for (int j=0; j < 8; j++) {
      char c=src.charAt(i * 8 + j);
      val|=(((int)(c - '0')) << (7 - j));
    }
    res[i]=(byte)val;
  }
  return res;
}
