{
  int index=-1;
  for (int i=0; i < items.size(); i++) {
    if (items.get(i).getListId() == id) {
      index=i;
      break;
    }
  }
  if (index >= 0) {
    items.remove(index);
    model.fireRemoved(index,index);
  }
}
