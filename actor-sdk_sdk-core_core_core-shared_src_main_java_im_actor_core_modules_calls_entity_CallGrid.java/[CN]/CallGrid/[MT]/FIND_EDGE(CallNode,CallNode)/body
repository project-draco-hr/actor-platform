{
  return new Predicate<CallGridEdge>(){
    @Override public boolean apply(    CallGridEdge callGridEdge){
      return (callGridEdge.getStart() == a && callGridEdge.getEnd() == b) || (callGridEdge.getEnd() == a && callGridEdge.getStart() == b);
    }
  }
;
}
