{
  FileCipher cipher=new FileCipher(key);
  try {
    cipher.decrypt(sourceFile,destFile);
  }
 catch (  IOException e) {
    e.printStackTrace();
    throw new RuntimeException(e);
  }
  return result(true);
}
