{
  boolean isRemoved=false;
  for (  Index i : indexes) {
    if (i.getId() == id) {
      indexes.remove(i);
      isRemoved=true;
      break;
    }
  }
  if (isRemoved) {
    updateIndex();
  }
}
