{
  ArrayList<MessageRef> res=new ArrayList<MessageRef>();
  outer:   for (  MessageRef ref : messages) {
    for (    Long l : rids) {
      if (ref.getRid() == l) {
        res.add(ref);
        continue outer;
      }
    }
  }
  return res;
}
