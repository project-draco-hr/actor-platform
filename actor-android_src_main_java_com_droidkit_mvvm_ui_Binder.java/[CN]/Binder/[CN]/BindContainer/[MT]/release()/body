{
  if (enabled != null) {
    enabled.release();
    enabled=null;
  }
  if (checked != null) {
    checked.release();
    checked=null;
  }
  if (visible != null) {
    visible.release();
    visible=null;
  }
  if (text != null) {
    text.release();
    text=null;
  }
  if (value != null) {
    value.release();
    value=null;
  }
  for (  Bind b : customs) {
    b.release();
  }
  customs.clear();
}
