{
  if (item.getItemId() == R.id.edit) {
    startActivity(new Intent(getContext(),GroupEditActivity.class).putExtra(Intents.EXTRA_GROUP_ID,groupVM.getId()));
    return true;
  }
 else {
    return super.onOptionsItemSelected(item);
  }
}
