{
  return PromisesArray.ofPromises(t1.cast(),t2.cast()).zip().map(new Function<List<Object>,Tuple2<T1,T2>>(){
    @Override public Tuple2<T1,T2> apply(    List<Object> src){
      return new Tuple2<>((T1)src.get(0),(T2)src.get(1));
    }
  }
);
}
