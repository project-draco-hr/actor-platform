{
  return new Predicate<MasterCallMember>(){
    @Override public boolean apply(    MasterCallMember masterCallMember){
      return masterCallMember.getUid() == uid && masterCallMember.getDeviceId().contains(deviceId);
    }
  }
;
}
