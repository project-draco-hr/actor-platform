{
  im.actor.model.api.User w=getWrapped();
  im.actor.model.api.User res=new im.actor.model.api.User(w.getId(),w.getAccessHash(),w.getName(),w.getLocalName(),w.getSex(),avatar,w.getContactInfo(),w.isBot());
  res.setUnmappedObjects(w.getUnmappedObjects());
  return new User(res);
}
