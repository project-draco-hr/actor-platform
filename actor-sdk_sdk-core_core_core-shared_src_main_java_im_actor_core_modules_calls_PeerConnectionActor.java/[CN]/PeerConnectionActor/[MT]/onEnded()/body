{
  Log.d(TAG,"OnEnded");
  if (peerConnection != null) {
    peerConnection.close();
    peerConnection=null;
  }
  if (stream != null) {
    stream.close();
    stream=null;
  }
  isReady=false;
  state=State.CLOSED;
  self().send(PoisonPill.INSTANCE);
}
