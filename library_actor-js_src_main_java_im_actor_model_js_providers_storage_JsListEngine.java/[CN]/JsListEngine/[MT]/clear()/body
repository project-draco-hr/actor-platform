{
  cache.clear();
  storage.clear();
  for (  JsListEngineCallback<T> callback : callbacks) {
    try {
      callback.onClear();
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
}
