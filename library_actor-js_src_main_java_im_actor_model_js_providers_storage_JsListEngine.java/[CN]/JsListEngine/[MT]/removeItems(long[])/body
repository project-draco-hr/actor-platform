{
  for (  long key : keys) {
    cache.remove(key);
  }
  storage.delete(keys);
  for (  JsListEngineCallback<T> callback : callbacks) {
    try {
      callback.onItemsRemoved(keys);
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
}
