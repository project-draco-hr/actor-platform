{
  checkSqlite();
  Cursor mCount=null;
  try {
    mCount=db.rawQuery("SELECT COUNT(*) FROM \"" + name + "\"",null);
    if (mCount.moveToFirst()) {
      return mCount.getInt(0);
    }
  }
  finally {
    if (mCount != null) {
      mCount.close();
    }
  }
  return 0;
}
