{
  long rid=RandomUtils.nextRid();
  Message message=new Message(rid,date,date,uid,MessageState.UNKNOWN,new ServiceUserRegistered());
  ownReadActor().send(new OwnReadActor.NewMessage(new Peer(PeerType.PRIVATE,uid),rid,date));
  conversationActor(Peer.user(uid)).send(message);
}
