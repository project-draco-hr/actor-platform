{
  if (item.getItemId() == R.id.done) {
    if (getSelectedCount() > 0) {
      execute(messenger().createGroup(title,avatarPath,BoxUtil.unbox(getSelected())),R.string.progress_common,new CommandCallback<Integer>(){
        @Override public void onResult(        Integer res){
          getActivity().startActivity(Intents.openGroupDialog(res,true,getActivity()));
          getActivity().finish();
        }
        @Override public void onError(        Exception e){
        }
      }
);
    }
    return true;
  }
  return super.onOptionsItemSelected(item);
}
