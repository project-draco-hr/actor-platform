{
  PowerManager powerManager=(PowerManager)AndroidContext.getContext().getSystemService(Context.POWER_SERVICE);
  wakeLock=powerManager.newWakeLock(PowerManager.PARTIAL_WAKE_LOCK,"ActorWakelock");
  wakeLock.acquire();
}
