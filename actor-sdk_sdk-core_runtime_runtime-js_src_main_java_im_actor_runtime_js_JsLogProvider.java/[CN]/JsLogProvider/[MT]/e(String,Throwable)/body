{
  String stackTrace="";
  for (  StackTraceElement element : throwable.getStackTrace()) {
    stackTrace+=element + "\n";
  }
  error(formatTime() + "[E] " + tag+ ":"+ throwable.getMessage()+ "\n"+ stackTrace);
}
