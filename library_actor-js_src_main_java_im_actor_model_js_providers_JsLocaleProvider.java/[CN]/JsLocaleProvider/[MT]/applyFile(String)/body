{
  String[] lines=text.split("\n");
  for (  String s : lines) {
    s=s.trim();
    if (s.length() == 0) {
      continue;
    }
 else     if (s.startsWith("#")) {
      continue;
    }
    String[] kv=s.split("=",2);
    locale.put(kv[0].trim(),kv[1].trim());
  }
}
