{
  if (avatarImage == null) {
    return null;
  }
  return new AvatarImage(avatarImage.getWidth(),avatarImage.getHeight(),convert(avatarImage.getFileLocation(),"avatar.jpg",avatarImage.getFileSize()));
}
