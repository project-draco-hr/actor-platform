{
  if (callback == null) {
    callback=new CallsModule.CallCallback(){
      @Override public void onCallEnd(){
      }
      @Override public void onSignal(      byte[] data){
      }
    }
;
  }
}
