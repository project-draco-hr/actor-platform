{
  if (encryptor == null) {
synchronized (LOCK) {
      if (encryptor == null) {
        encryptor=TypedCreator.typed(ActorSystem.system().actorOf(Props.create(RsaActor.class).changeDispatcher("rsa"),"rsa"),RsaEncryptor.class);
      }
    }
  }
  return encryptor;
}
