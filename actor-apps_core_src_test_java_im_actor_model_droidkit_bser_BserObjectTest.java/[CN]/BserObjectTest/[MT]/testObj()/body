{
  new EmptyBserObj().toByteArray();
  try {
    new BserObject(){
      @Override public void parse(      BserValues values) throws IOException {
      }
      @Override public void serialize(      BserWriter writer) throws IOException {
        throw new IOException();
      }
    }
.toByteArray();
    throw new AssertionError();
  }
 catch (  Exception e) {
  }
}
