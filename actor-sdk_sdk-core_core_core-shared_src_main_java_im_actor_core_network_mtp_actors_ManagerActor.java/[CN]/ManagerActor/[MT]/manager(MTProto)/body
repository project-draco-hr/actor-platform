{
  return ActorSystem.system().actorOf(new ActorSelection(Props.create(new ActorCreator(){
    @Override public ManagerActor create(){
      return new ManagerActor(mtProto);
    }
  }
).changeDispatcher("network_manager"),mtProto.getActorPath() + "/manager"));
}
