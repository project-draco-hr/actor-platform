{
  query=query.trim();
  if (query.length() == 0) {
    if (this.query != null) {
      if (clear) {
        clearList();
      }
      initForward(false);
    }
  }
 else {
    if (clear) {
      clearList();
    }
    initForwardSearch(query);
  }
}
