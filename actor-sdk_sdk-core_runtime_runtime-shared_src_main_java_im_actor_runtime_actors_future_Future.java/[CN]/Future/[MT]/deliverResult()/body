{
  if (exception != null) {
    for (    FutureCallback<T> callback : callbacks) {
      callback.onError(exception);
    }
  }
 else {
    for (    FutureCallback<T> callback : callbacks) {
      callback.onResult(result);
    }
  }
}
