{
  if (dialogsList == null) {
    dialogsList=new AngularList<JsDialog,Dialog>((JsListEngine<Dialog>)modules().getMessagesModule().getDialogsEngine(),JsDialog.CONVERTER,modules().getI18nEngine());
  }
  return dialogsList;
}
