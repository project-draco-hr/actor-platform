{
  ApiUser w=getWrapped();
  ApiUser res=new ApiUser(w.getId(),w.getAccessHash(),w.getName(),w.getLocalName(),w.getSex(),w.getAvatar(),w.getContactInfo(),w.isBot(),nick,w.getAbout(),w.getExternal(),w.getPreferredLanguages(),w.getTimeZone(),w.getBotCommands());
  res.setUnmappedObjects(w.getUnmappedObjects());
  return new User(res);
}
