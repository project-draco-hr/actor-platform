{
  Object object=this.get(index);
  if (object instanceof im.actor.model.droidkit.json.JSONObject) {
    return (im.actor.model.droidkit.json.JSONObject)object;
  }
  throw new JSONException("JSONArray[" + index + "] is not a JSONObject.");
}
