{
  View res=inflate(inflater,container,R.layout.fragment_all_messages,messenger().getCustomDisplayList(new Peer(PeerType.PRIVATE,1),"favorite",MessageEx.CREATOR));
  res.setBackgroundColor(ActorSDK.sharedActor().style.getBackyardBackgroundColor());
  return res;
}
