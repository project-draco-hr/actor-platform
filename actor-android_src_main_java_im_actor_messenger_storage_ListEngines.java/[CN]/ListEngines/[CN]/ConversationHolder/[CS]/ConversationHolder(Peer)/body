{
  SQLiteStorageAdapter storageAdapter=new SQLiteStorageAdapter(SQLiteProvider.db(),"MESSAGES_" + peer.getUid());
  engine=new ListEngine<Message>(storageAdapter,new MessagesAdapter());
  uiList=new EngineUiList<Message>(engine,20,false);
  uiList.initGeneral();
}
