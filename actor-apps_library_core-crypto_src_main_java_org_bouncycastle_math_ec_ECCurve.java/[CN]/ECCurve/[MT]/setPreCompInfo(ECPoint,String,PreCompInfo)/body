{
  checkPoint(point);
synchronized (point) {
    HashMap table=point.preCompTable;
    if (null == table) {
      point.preCompTable=table=new HashMap(4);
    }
    table.put(name,preCompInfo);
  }
}
