{
  Dialog dialog=dialogs.getValue(peer.getUnuqueId());
  if (dialog != null) {
    dialogs.addOrUpdateItem(new DialogBuilder(dialog).setMessageType(ContentType.EMPTY).setText(null).setTime(0).setUnreadCount(0).setRid(0).createDialog());
  }
}
