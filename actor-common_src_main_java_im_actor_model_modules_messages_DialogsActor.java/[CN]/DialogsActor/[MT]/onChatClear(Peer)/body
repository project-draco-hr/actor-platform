{
  Dialog dialog=dialogs.getValue(peer.getUid());
  if (dialog != null) {
    dialogs.addOrUpdateItem(new DialogBuilder(dialog).setMessageType(Dialog.ContentType.EMPTY).setText(null).setTime(0).setUnreadCount(0).setRid(0).createDialog());
  }
}
