{
  TerminalSize preferredSize=border.surroundAreaSize(layoutManager.getPreferredSize());
  if (title.length() + 4 > preferredSize.getColumns())   preferredSize.setColumns(title.length() + 4);
  return preferredSize;
}
