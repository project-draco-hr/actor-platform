{
  if (!messagesList.containsKey(peer)) {
    messagesList.put(peer,new AngularList<JsMessage,Message>((JsListEngine<Message>)modules().getMessagesModule().getConversationEngine(peer),true,JsMessage.CONVERTER,messenger));
  }
  return messagesList.get(peer);
}
