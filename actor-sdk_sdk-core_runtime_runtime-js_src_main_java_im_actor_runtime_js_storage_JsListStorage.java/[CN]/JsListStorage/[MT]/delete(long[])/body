{
  for (  long key : keys) {
    for (    Index i : index) {
      if (i.id == key) {
        index.remove(i);
        storage.removeItem(getId(key));
        saveIndex();
        break;
      }
    }
  }
}
