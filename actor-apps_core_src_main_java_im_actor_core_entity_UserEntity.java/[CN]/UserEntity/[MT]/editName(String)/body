{
  im.actor.core.api.User w=getWrapped();
  im.actor.core.api.User res=new im.actor.core.api.User(w.getId(),w.getAccessHash(),name,w.getLocalName(),w.getSex(),w.getAvatar(),w.getContactInfo(),w.isBot(),w.getNick(),w.getAbout());
  res.setUnmappedObjects(w.getUnmappedObjects());
  return new UserEntity(res);
}
